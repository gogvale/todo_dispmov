# Add your own group_tasks in files placed in lib/group_tasks ending in .rake,
# for example lib/group_tasks/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
